#include <bits/stdc++.h>
using namespace std;

void printV(vector<int> V) {
    for (auto i: V) {
        cout << i << "\n";
    }
}

int main() {
    ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);

    cout << "hello, world!" << "\n";
    return 0;
}