#include <bits/stdc++.h>
using namespace std;

void printV(vector<int> V) {
    for (auto i: V) {
        cout << i << "\n";
    }
}

int main() {
    ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);

    cout << "hello, world!" << "\n";
    return 0;
}